rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Basic baseline rule: Require authentication for all read/write operations
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}